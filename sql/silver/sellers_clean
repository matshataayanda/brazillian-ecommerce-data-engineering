CREATE TABLE silver.sellers_clean AS
SELECT DISTINCT
    TRIM(seller_id) AS seller_id,
    LOWER(TRIM(seller_city)) AS seller_city,
    UPPER(TRIM(seller_state)) AS seller_state
FROM bronze.sellers_raw
WHERE seller_id IS NOT NULL